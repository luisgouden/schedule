html, body {
  width: 100%;
  height: 100%;
  margin: 0;
  padding: 0;
}

html {
  box-sizing: border-box;
  min-height: 100vh;

  @media (--medium) {
      min-height: var(--maxwidth);
  }

}

body{
  font-family:"system-ui",sans-serif;
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  align-items: center;
}

#svelte {
  display: flex;
  flex-direction: column;
  width: 96%;

  @media (--medium) {

    &.column{
      max-width: var(--maxwidth);
    }
  
    &.row {
      flex-direction: row;
      width: 100%;
      padding-right: 1rem;
    }
  }

}

main {
  display: flex;
  flex-direction: column;
  justify-content: flex-start;
  align-items: center;  
  max-width: var(--maxwidth);
}

article, main {
  width: 100%;
}

ul {
  padding: 0;

  & li {
    list-style-type: none;
    list-style: none;
  }

}

*, :after, :before {
  box-sizing: inherit;
}

a{
  text-decoration: none;
}
